# 🚀 Деплой Telegram бота

После тестирования локально, вы можете развернуть бота на сервере, чтобы он работал 24/7.

## 📋 Варианты деплоя

### 🔵 Вариант 1: VPS (Ubuntu/Debian) - Рекомендуется

**Подходит для:** Полный контроль, стабильная работа

**Стоимость:** ~$5-10/месяц (DigitalOcean, Hetzner, Vultr)

#### Шаг 1: Подключение к серверу

```bash
ssh your_user@your_server_ip
```

#### Шаг 2: Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

#### Шаг 3: Установка зависимостей

```bash
# Python и pip
sudo apt install python3 python3-pip git -y

# Проверка версии Python (должна быть >= 3.9)
python3 --version
```

#### Шаг 4: Клонирование проекта

```bash
cd ~
git clone https://github.com/your-username/levelfit.git
cd levelfit/bot
```

Или загрузите файлы через SFTP/SCP.

#### Шаг 5: Установка зависимостей Python

```bash
pip3 install -r requirements.txt
```

#### Шаг 6: Создание .env файла

```bash
nano .env
```

Вставьте ваши токены:
```env
BOT_TOKEN=ваш_токен
PAYMENT_TOKEN=ваш_платежный_токен
ADMIN_TELEGRAM_ID=ваш_id
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

#### Шаг 7: Создание systemd сервиса

```bash
# Отредактируйте файл сервиса
nano levelfit-bot.service
```

Замените:
- `/path/to/levelfit` на реальный путь (например `/home/your_user/levelfit`)
- `your_username` на ваше имя пользователя

```bash
# Скопируйте файл в systemd
sudo cp levelfit-bot.service /etc/systemd/system/

# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable levelfit-bot

# Запустите бота
sudo systemctl start levelfit-bot

# Проверьте статус
sudo systemctl status levelfit-bot
```

#### Шаг 8: Просмотр логов

```bash
# Логи в реальном времени
sudo journalctl -u levelfit-bot -f

# Последние 100 строк
sudo journalctl -u levelfit-bot -n 100
```

#### Управление сервисом

```bash
# Остановить
sudo systemctl stop levelfit-bot

# Перезапустить
sudo systemctl restart levelfit-bot

# Статус
sudo systemctl status levelfit-bot

# Отключить автозапуск
sudo systemctl disable levelfit-bot
```

---

### 🐳 Вариант 2: Docker

**Подходит для:** Изоляция, простое обновление

#### Установка Docker

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

#### Запуск бота

```bash
cd ~/levelfit/bot

# Создайте .env файл
nano .env

# Запуск через docker-compose
docker-compose up -d

# Проверка логов
docker-compose logs -f

# Остановка
docker-compose down
```

#### Обновление

```bash
# Остановка
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose up -d --build
```

---

### ☁️ Вариант 3: Heroku (бесплатно до 550 часов/месяц)

**Подходит для:** Быстрый старт, без сервера

#### Шаг 1: Установка Heroku CLI

```bash
curl https://cli-assets.heroku.com/install.sh | sh
```

#### Шаг 2: Логин в Heroku

```bash
heroku login
```

#### Шаг 3: Создание приложения

```bash
cd ~/levelfit/bot

# Создание приложения
heroku create levelfit-bot

# Установка переменных окружения
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set PAYMENT_TOKEN=ваш_платежный_токен
heroku config:set ADMIN_TELEGRAM_ID=ваш_id
```

#### Шаг 4: Создание Procfile

```bash
echo "worker: python bot.py" > Procfile
```

#### Шаг 5: Деплой

```bash
git init
git add .
git commit -m "Initial commit"
git push heroku main

# Запуск worker dyno
heroku ps:scale worker=1

# Проверка логов
heroku logs --tail
```

---

### 🌐 Вариант 4: Railway.app (простой деплой)

**Подходит для:** Простота, современный интерфейс

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Выберите папку `bot/`
4. Добавьте переменные окружения в настройках
5. Railway автоматически развернет бота

---

### 🔧 Вариант 5: Render.com

**Подходит для:** Бесплатный хостинг

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый **Background Worker**
3. Подключите GitHub репозиторий
4. Установите:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
5. Добавьте переменные окружения
6. Deploy!

---

## 🔍 Мониторинг

### Проверка работы бота

```bash
# VPS с systemd
sudo systemctl status levelfit-bot

# Docker
docker-compose ps
docker-compose logs --tail 50

# Heroku
heroku ps
heroku logs --tail
```

### Telegram Bot API статус

Проверьте, что ваш бот онлайн:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

---

## 🛠 Решение проблем

### Бот не запускается

1. Проверьте логи:
```bash
# Systemd
sudo journalctl -u levelfit-bot -n 50

# Docker
docker-compose logs
```

2. Проверьте токены в `.env`
3. Убедитесь, что установлены все зависимости

### Бот падает при платежах

1. Проверьте PAYMENT_TOKEN
2. Убедитесь, что провайдер подключен
3. Проверьте баланс в YooKassa/ЮMoney

### Высокая нагрузка CPU/RAM

1. Ограничьте ресурсы в systemd:
```ini
MemoryMax=256M
CPUQuota=30%
```

2. Или в docker-compose:
```yaml
deploy:
  resources:
    limits:
      memory: 256M
      cpus: '0.3'
```

---

## 📊 Рекомендации по серверам

### Минимальные требования:
- CPU: 1 ядро
- RAM: 512 MB
- Диск: 2 GB
- ОС: Ubuntu 20.04+ или Debian 11+

### Провайдеры:

**Бюджетные ($3-5/месяц):**
- Hetzner Cloud
- Vultr
- DigitalOcean

**Бесплатные:**
- Oracle Cloud (Always Free tier)
- Google Cloud (Free tier)
- AWS (Free tier 12 месяцев)

**Специально для ботов:**
- Railway.app
- Render.com
- Fly.io

---

## 🔐 Безопасность

1. **Не публикуйте .env в Git**
   - Уже добавлен в .gitignore
   
2. **Используйте SSH ключи вместо паролей**
```bash
ssh-keygen -t ed25519
ssh-copy-id user@server
```

3. **Настройте файрвол**
```bash
sudo ufw allow 22/tcp
sudo ufw enable
```

4. **Регулярно обновляйте систему**
```bash
sudo apt update && sudo apt upgrade -y
```

5. **Регулярно меняйте токены**

---

## 📈 Масштабирование

Когда клиентов станет много:

1. **База данных:** Добавьте PostgreSQL или MongoDB для хранения подписок
2. **Кэширование:** Используйте Redis для кэша
3. **Очереди:** RabbitMQ или Celery для обработки платежей
4. **Балансировка:** Несколько инстансов бота

---

## 🆘 Поддержка

Если что-то не работает:
1. Проверьте логи
2. Убедитесь, что все токены корректны
3. Перезапустите бота
4. Проверьте интернет-соединение на сервере

---

**Готово! Ваш бот работает 24/7! 🎉**


