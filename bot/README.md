# LEVEL FIT Telegram Bot

Telegram бот для обработки платежей и подписок на тарифы LEVEL FIT.

## 🚀 Быстрый старт

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например, "LEVEL FIT")
4. Придумайте username для бота (например, "levelfit_payment_bot")
5. Получите токен бота и сохраните его

### 2. Настройка платежей

#### Вариант A: YooKassa (рекомендуется для России)

1. Зарегистрируйтесь на [YooKassa](https://yookassa.ru/)
2. Получите Secret Key в личном кабинете
3. Подключите Telegram Payments в настройках магазина
4. Получите Payment Token

#### Вариант B: Telegram Payment Provider (для международных платежей)

1. Зарегистрируйтесь у одного из провайдеров:
   - Stripe
   - PayPal
   - другие поддерживаемые провайдеры
2. Свяжите провайдера с вашим ботом через @BotFather:
   - Отправьте `/mybots`
   - Выберите вашего бота
   - "Payments" → выберите провайдера
   - Введите данные от провайдера

### 3. Установка зависимостей

```bash
cd bot
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и заполните:
```env
BOT_TOKEN=ваш_токен_от_BotFather
PAYMENT_TOKEN=ваш_платежный_токен
ADMIN_TELEGRAM_ID=ваш_telegram_id
```

Чтобы узнать свой Telegram ID:
- Напишите боту [@userinfobot](https://t.me/userinfobot)

### 5. Запуск бота

```bash
python bot.py
```

## 📝 Настройка ссылок на сайте

После запуска бота, обновите кнопки на сайте, чтобы они вели на вашего бота.

Формат ссылки для автоматического выбора тарифа:
```
https://t.me/ваш_бот_username?start=план
```

Где `план` может быть:
- `light` - тариф ЛАЙТ
- `start` - тариф СТАРТ
- `optimal` - тариф ОПТИМА
- `vip` - тариф ПРЕМИУМ VIP

Примеры:
- `https://t.me/levelfit_payment_bot?start=light`
- `https://t.me/levelfit_payment_bot?start=optimal`

## 🔧 Структура проекта

```
bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── .env.example       # Пример файла с переменными окружения
├── .env               # Ваши настройки (не коммитить!)
└── README.md          # Этот файл
```

## 💳 Тестирование платежей

Для тестирования используйте тестовые данные карт, предоставленные вашим платежным провайдером.

### YooKassa тестовые карты:
- **Успешный платеж:** `5555 5555 5555 4477`
- **Отклоненный платеж:** `5555 5555 5555 4444`
- Срок действия: любая будущая дата
- CVV: любые 3 цифры

## 📱 Команды бота

- `/start` - Начать работу с ботом
- `/plans` - Показать все доступные тарифы
- `/help` - Помощь

## 🛠 Деплой на сервер (опционально)

### Вариант 1: VPS (Ubuntu/Debian)

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip -y

# Клонирование проекта
git clone <your-repo> && cd levelfit/bot

# Установка зависимостей
pip3 install -r requirements.txt

# Создание systemd сервиса
sudo nano /etc/systemd/system/levelfit-bot.service
```

Содержимое сервиса:
```ini
[Unit]
Description=LEVEL FIT Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/levelfit/bot
ExecStart=/usr/bin/python3 /path/to/levelfit/bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable levelfit-bot
sudo systemctl start levelfit-bot
sudo systemctl status levelfit-bot
```

### Вариант 2: Docker

Создайте `Dockerfile`:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

Запуск:
```bash
docker build -t levelfit-bot .
docker run -d --name levelfit-bot --env-file .env levelfit-bot
```

### Вариант 3: Heroku

```bash
# Установка Heroku CLI
# https://devcenter.heroku.com/articles/heroku-cli

heroku login
heroku create levelfit-bot
heroku config:set BOT_TOKEN=your_token
heroku config:set PAYMENT_TOKEN=your_payment_token
heroku config:set ADMIN_TELEGRAM_ID=your_id
git push heroku main
```

## 📊 Логи

Бот логирует все важные события:
- Успешные платежи
- Ошибки при создании инвойсов
- Информацию о новых подписчиках

## 🔒 Безопасность

1. **Никогда** не коммитьте файл `.env` в Git
2. Используйте сильные токены
3. Регулярно обновляйте зависимости
4. Храните токены в безопасном месте

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что все токены указаны правильно
3. Проверьте, что бот имеет права на отправку инвойсов
4. Проверьте баланс платежного провайдера

## 📄 Лицензия

© 2024 LEVEL FIT. Все права защищены.


